# Versions here are the ones this project is actually verified against, taken
# from a working environment rather than chosen by hand. The previous file
# pinned releases that were never installed, so a fresh clone would have built
# a different environment from the one the tests were run in.

# --- API server ---
fastapi==0.141.1
uvicorn[standard]==0.52.4
# Required by FastAPI for File() and Form() parameters. Without it the app
# raises on startup rather than at request time, so /api/voice cannot even load.
python-multipart==0.0.32

# --- Validation ---
pydantic==2.13.4

# --- Embeddings (quantized ONNX, local CPU) ---
fastembed==0.8.0
onnxruntime==1.29.0

# --- Vector index ---
faiss-cpu==1.15.0
numpy==2.5.2

# --- Dataset access ---
datasets==5.0.1
# Imported directly to stream parquet row groups, so it is declared explicitly
# rather than relied on as a transitive dependency of datasets.
pyarrow==25.0.1
# Also imported directly, for resumable model and dataset downloads.
huggingface_hub==1.28.0

# --- HTTP client for Groq and Sarvam ---
httpx==0.28.1

# --- Configuration ---
python-dotenv==1.2.3

# --- Tests ---
pytest==9.1.1
pytest-asyncio==1.4.0
