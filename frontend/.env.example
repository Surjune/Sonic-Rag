# Leave unset in development: vite.config.ts proxies /api and /health to the
# backend, which keeps the browser on one origin so microphone permission and
# CORS behave predictably.
#
# Set this for a production build where the API lives on another host.
# VITE_API_BASE=https://your-backend.hf.space
